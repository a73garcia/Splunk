

index=siem-cloud-ciscoesa host="*.santandergroup.c3s2.iphmx.com" signature="*Exmos. Senhores,\*"
| eval valores = split(signature,";")
| eval regla = mvindex(valores,0)
| eval fecha = mvindex(valores,1)
| eval IP = mvindex(valores,4)
| eval Equipo = mvindex(valores,10)
| eval Origen_C = mvindex(valores,11)
| eval Sender = trim(mvindex(split(Origen_C,"="),1), "\"")
| eval Destino_C = mvindex(valores,12)
| table regla fecha IP Equipo Sender Destino_C



