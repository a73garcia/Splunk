replace(
  replace(
    replace(
      replace(variables('Texto'),'https://',''),
      'http://',''),
    'www.',''),
  '.com',''
)

replace(outputs('Componer_(Eliminar_URLs)'), '\n', '<br>')


@{outputs('Componer_(Mantener_formato)')}
